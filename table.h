#pragma once

#include <map>
#include <vector>
#include <string>
#include <iostream>

#include "debug_settings.h"

namespace table {

  std::string receive(const char* data, std::size_t size);
}
